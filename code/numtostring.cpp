#include <stdio.h>

char *  numtostring(int number);

int main(void)
{
    int number=0;

    printf("\n: %s\n", numtostring(number));

    return 0;
}

char* numtostring(int number)
{
    char *pstring=NULL;

    return pstring;
}
