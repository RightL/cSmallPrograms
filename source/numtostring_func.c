#include <stdio.h>
#include <stdarg.h>

char *to_string(size_t count, double first, ...);

int main(void)
{

        return 0;
}

char *to_string(size_t count, double first, ...)
{
        char *string = NULL;
        return string;
}
